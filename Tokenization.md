# 分词算法
## Byte-Pair-Encoding (BPE)
<strong>核心思想</strong>：将词元进行配对，统计频次最高的配对组合，将其作为新的词元添入词表<br>
<strong>应用模型</strong>：GPT-2、GPT-3、LLaMA、RoBERTa、BART<br>
### :v:算法相关

<strong>初始词表</strong>：词元均为单个字符<br>
<strong>步骤</strong>：<br>
<ol>
<li>将语料库划分为单个单词，统计单词出现频次</li>
<li>将单词根据现词表拆分为多个词元，统计词元对频次，将最高频次的组合添入词表。并用新的词元替换部分原划分。如下</li>
<img width="865" height="351" alt="image" src="https://github.com/user-attachments/assets/60032e38-2d99-49fb-9ee1-35aebb65353b" />
<li>重复以上步骤，直到达到预设词表大小/合并次数或合并不再有意义</li>
</ol>

```python
# 初始词表
0:'a'
1:'b'
2:'+'
...
10:'r'
# 更新一次后
11:'a+'
```

<strong>其他</strong>：<br>
<ul>
<li>集外词（OOV）字符会被标记为[UNK]</li>
<li>用&lt;/w&gt;表示词边界。（1）可以引入部分语境信息：比如"t&lt;/w&gt;"和"t"分别表示单词末尾和非末尾的字符t。同理，这样就可以将有意义的单词后缀与中间位置的同组合区别开来。（2）提供了边界信息</li>
</ul>
<br>

## Byte-level BPE (BBPE)
<strong>核心思想</strong>：词元最小单位是字节，而不是字符<br>
### UTF-8
一种可变长度的字符编码方案，专门用于编码**Unicode**（一种字符集，为每一个字符分配了一个唯一的标识，也叫代码点）字符集，将每一个字符的唯一标识编码为1-4个字节长度的字节序列<br><br>
<strong>编码规则</strong>：
| 代码点范围               | 编码后序列长度                               | 编码序列格式 | 其他 |
|------------------       |----|--------                                  |------|
| **U+0000到U+007F**     |1字节| `0xxxxxxx`                             | 完全兼容ASCII |
| **U+0080到U+07FF**     |2字节| `110xxxxx 10xxxxxx`                    | |
| **U+0800到U+FFFF**     |3字节| `1110xxxx 10xxxxxx 10xxxxxx`           | |
| **U+10000到U+10FFFF**  |4字节| `11110xxx 10xxxxxx 10xxxxxx 10xxxxxx`  |  |

**例如**：
字符'你'的Unicode码是U+4F60，二进制形式为0100 1111 0110 0000，对照3字节格式是：1110`0100` 10`111101` 10`100000`，转换为16进制为：0xe4bda0

### :v:算法相关
<strong>初始词表</strong>：均为单个字节，一个字节由8位二进制数表示，故BBPE初始词表大小为256<br>
<strong>步骤</strong>：与BPE基本相同，只是词元表示形式从字符变为了字节，以及初始词表大小固定为了256<br>

```python
# 初始词表
0: b'\x00'
1: b'\x01'
2: b'\x02'
...
97: b'\x61' 
98: b'\x62'      
99: b'\x63'    
...
255: b'\xff'
# 更新几次后
257: b'\xe4\xbd\xa0'
259: b'\xe5\xa5\xbd'
260: b'\xe4\xbd\xa0\xe5\xa5\xbd'
```

<br>

## WordPiece
<strong>核心思想</strong>：类似于BPE，最大区别在于合并规则，根据合并分数来合并<br>
<strong>应用模型</strong>：BERT、ALBERT<br>


### :v:算法相关
<strong>初始词表</strong>：与BPE类似<br>
<strong>步骤</strong>：<br>
<ol>
<li>统计现词元和合并对的频次</li>
<li>计算合并分数： $$\frac{合并对的频次}{词元1的频次\times词元2的频次}$$ </li>
<li>将分数最高的组合合并，添入词表。重复以上步骤</li>
</ol>
<strong>其他</strong>：<br>
<ul>
<li>##前缀：初始词表中词元均无此前缀，在训练过程中如果发现合并对位于中间，则加上前缀，如变成"##ing"</li>
</ul>



## Unigram Language Model (ULM) 与SentencePiece
<strong>核心思想</strong>：与之前算法不同，SentencePiece不对文本进行预分割，形成单词集合，而是将整个文本视为连续的字符串流<br>
<strong>应用模型</strong>：AlBERT、T5、mBART、Big Bird 和 XLNet 等<br>


### :v:算法相关
<strong>初始词表</strong>：从大型初始词表开始，从中删除词元以至最终达到预设大小<br>
<strong>步骤</strong>：<br>
<ol>
<li>构建初始词表，可通过获取原始完整一个单词中常见的子字符串（生成初始完整的一个单词的所有可能切分子词，如hug切分为h u g hu ug hug，这是一个极端的例子，实际还要考虑子词是否比较常见）或利用BPE并设定较大的大小</li>
<li>根据当前词汇表计算总损失（初始整体损失）？再计算每一个字符被删除后对总损失的增加值，对增加值贡献最少的符号，是最不重要的。为了加快训练速度，通常会删除整体前p%（p一般为10或20）个增加值最小的词元。但是从不删除基本字符，以确保任何单词都可以被分词。这一大步是通过ULM实现的</li>
</ol>
<strong>分词算法ULM</strong>：<br>
<ol>
<li>它假设句子中每个词元都是独立的，这为它的算法中句子的概率为词元概率之积行了方便，与词袋思想类似，定义词元的概率与上下文无关，其概率为出现频次除以所有词元的出现次数之和</li>
<li>考虑所有可能分词形式构建初始词表</li>
<li>计算每个词元的概率，计算句子的出现概率（所有相乘），选择概率最高的方式。一般来讲，词元数量越少，乘积结果越大。</li>
<li>计算该概率使用Viterbi算法：构建图表示所有可能分词方式，如果字符a到b的子词ab存在，就在字符a和b之间建立边，边权重定义为该子词的概率。</li>
<li>整体损失值：根据每个单词的分词方式进行每个词元的负对数似然值求和，得到该单词的得分。整体损失值为每个单词得分*该单词频次之和</li>
<li>计算移除某个词元的损失影响：移除某个词元，那么有些单词的分词形式就会变化，单词得分也就变化，以此对整体的损失值产生影响</li>
1、但对于较大的子字符串，我们只保留最常见的子字符串，因此我们按频率对其进行排序，保留最高频的长子串。所以初始词表包含所有基本单元和高频长子串（这是一种构建方式之一）
2、循环以上操作，直到达到预设大小
```python
字符 0 (u): "u" (得分 0.171429)
字符 1 (n): "un" (得分 0.076191)
字符 2 (h): "un" "h" (得分 0.005442)
字符 3 (u): "un" "hu" (得分 0.005442)
字符 4 (g): "un" "hug" (得分 0.005442)
后三行结果只能是因为h hu只出现在hug里
```

</ol>
<strong>其他</strong>：<br>
<ul>
<li>与SentencePiece结合使用，SentencePiece解决了并非所有语言都使用空格分隔单词</li>
</ul>

维特比算法
<img width="2760" height="1482" alt="image" src="https://github.com/user-attachments/assets/a2fc7cdf-ea82-4631-80fc-13d54754449a" />
问题：
1、如果“意见”这个词元不存在于初始词表，那这样动态规划是不是就失败了
2、这个算法并不是完全的动态规划，和我认为的动态规划不一样。在计算时，无法直接依靠上一次的最佳分词结果，在此结果上继续分词，这个结果只能作为可能的一个参考，还是要考虑当前剩下的所有可能情况。不过这个确实可以筛除一些路径，比如分词方式"w" "o"得分低，那么就不会有"w" "o" "r"这一分词路径的考虑
3、维特比在篱笆网络形式的有向无环图中表现为完全动态规划
4、为了避免小数连乘溢出，改用-log（负对数似然）的形式，找概率最大分词方式相当于找最短路径
<img width="627" height="463" alt="image" src="https://github.com/user-attachments/assets/06379f12-7f84-4fe1-b634-ad8712958342" />


SentencePiece 使用一种更高效的算法，称为增强后缀数组 (ESA) 来创建初始词汇表。【待看】
